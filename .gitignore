# Dependencies
node_modules/
package-lock.json # Optional: some prefer to commit this, but if user wants to ignore it... usually we commit lock files. I will KEEP lock files unless user asked to ignore. Standard is to commit them.
# I will NOT ignore package-lock.json.

# Enviroment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build outputs
dist/
build/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS specific
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/

# Backend specific
backend/uploads/*
!backend/uploads/.gitkeep
